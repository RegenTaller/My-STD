#include <string>
#include <iostream>

#pragma once

using namespace std;

int** intarr(int, int);

string** stringarr(int, int);

double** doublearr(int, int);

int** arrin(int**, int, int);
